name: RDP-noVNC-Serveo-Fixed
on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Install Tools
        run: |
          choco install ultravnc python -y
          python -m pip install websockify
          mkdir C:\noVNC
          git clone https://github.com/novnc/noVNC.git C:\noVNC

      - name: Start UltraVNC
        run: |
          $vncPath = "C:\Program Files\uvnc bvba\UltraVNC"
          # Password is set to: password
          $regPath = "HKLM:\SOFTWARE\UltraVNC\Default"
          if (!(Test-Path $regPath)) { New-Item -Path $regPath -Force }
          New-ItemProperty -Path $regPath -Name "Password" -Value ([byte[]](0x5E,0x2B,0xD3,0x64,0x90,0x6F,0x8F,0x00)) -PropertyType Binary -Force
          Start-Process -FilePath "$vncPath\winvnc.exe" -ArgumentList "-run" -NoNewWindow

      - name: Start Websockify
        run: |
          Start-Process -FilePath "python.exe" -ArgumentList "-m websockify --web C:\noVNC 6080 localhost:5900" -NoNewWindow

      - name: Connect Serveo Tunnel
        shell: pwsh
        run: |
          Write-Host "Connecting to Serveo..."
          # We use a broad regex to catch both .serveo.net and .serveousercontent.com
          $sshProcess = Start-Process ssh -ArgumentList "-o StrictHostKeyChecking=no -R 80:localhost:6080 serveo.net" -NoNewWindow -PassThru -RedirectStandardOutput "serveo.log"
          
          Write-Host "Waiting for URL..."
          $url = $null
          for ($i=1; $i -le 10; $i++) {
              Start-Sleep -Seconds 5
              $content = Get-Content "serveo.log" -ErrorAction SilentlyContinue
              # Improved Regex to catch any https link provided by Serveo
              $match = $content | Select-String -Pattern "https://[a-zA-Z0-9.-]+" 
              if ($match) {
                  $url = $match.Matches.Value.Trim()
                  break
              }
              Write-Host "Checking logs... attempt $i"
          }
          
          if ($url) {
              Write-Host "=========================================="
              Write-Host "SUCCESS! ACCESS WINDOWS HERE:"
              Write-Host "$url/vnc.html?autoconnect=true"
              Write-Host "VNC Password: password"
              Write-Host "=========================================="
          } else {
              Write-Host "--- DEBUG LOG CONTENT ---"
              Get-Content "serveo.log"
              throw "URL Extraction failed."
          }

      - name: Keep Alive
        run: |
          while ($true) {
            Start-Sleep -Seconds 300
            Write-Host "Still running..."
          }
